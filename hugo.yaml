baseURL: "https://ruyaohui.com"
title: "RiverYu's Lab"
languageCode: "zh-cn"
theme: "PaperMod"

# 这里的逻辑是：让文章链接直接使用文件名，去掉年月日文件夹
permalinks:
  projects: /projects/:contentbasename/
  notes: /notes/:contentbasename/
  posts: /posts/:contentbasename/

enableRobotsTXT: true
buildDrafts: false
buildFuture: false

build:
  writeStats: true

# ✅ 新增：强制开启资源压缩（HTML/CSS/JS/XML 都会被压缩去空格，体积变小）
minify:
  disableXML: false
  minifyOutput: true


outputs:
  home:
    - HTML
    - RSS
    - JSON

copyright: >
    &copy; 2025 RiverYu's Lab ·
    <span class="footer-motto">Per aspera ad astra</span>




params:
  env: production
  description: "Undergraduate CS Student | AI & 3D Editing, Deep Learning"
  author: "Yaohui Ru (RiverYu)"
  
  # ✅ 关键优化：资源精简
  assets:
    disableHLJS: true            # ⭐ 禁用 Highlight.js (JS库)，只用 Hugo 原生 Chroma (CSS)，大幅减少 JS 体积！
    disableFingerprinting: false # 保持指纹 (利于缓存)
    disableScrollBarStyle: true  # 移除自定义滚动条 CSS，减少代码量
    favicon: /river-logo.svg
    favicon16x16: /river-logo.svg
    favicon32x32: /river-logo.svg
    apple_touch_icon: /river-logo.svg
  
  sitemap:
    changefreq: weekly
    priority: 0.6

  math: true
  mainSections:
    - projects
    - notes
    - posts
  ShowToc: true
  TocOpen: true  # 默认展开目录，方便读者概览
  images: ["images/cover.png"]
  
  
  # 界面开关
  defaultTheme: auto 
  ShowReadingTime: true
  ShowShareButtons: false 
  ShowPostNavLinks: true
  ShowBreadCrumbs: true
  ShowCodeCopyButtons: true 
  ShowWordCount: true
  
  # 首页信息
  homeInfoParams:
    Title: "Hi, I'm RiverYu 👋"
    Content: |
      Welcome to my lab.

      I'm an undergraduate CS student (Network Engineering) at Henan Normal University.

      Currently focusing on **Computer Vision**.

      > *"Still waters run deep."*

  # 社交链接
  socialIcons:
    - name: github
      url: "https://github.com/railway02"
    - name: email
      url: "mailto:railway0206@gmail.com"

# 菜单架构 (确保 content 下有对应的文件！)
menu:
  main:
    - identifier: projects
      name: "Projects"
      url: /projects/
      weight: 10
      
    - identifier: notes
      name: "Notes"
      url: /notes/
      weight: 20

    - identifier: river
      name: "🌊 The River"
      url: /archives/
      weight: 30
      
    - identifier: search
      name: "Search"
      url: /search/
      weight: 40
      
    - identifier: about
      name: "About"
      url: /about/
      weight: 50

# 代码高亮
markup:
  goldmark:
    renderer:
      unsafe: true  # ✅ 允许在 Markdown/配置中使用 HTML 标签
  highlight:
    codeFences: true
    guessSyntax: true
    style: monokai
    noClasses: true
    tabWidth: 4